<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="Menu">
        <Setter Property="Background" Value="{DynamicResource MenuBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource MenuForegroundBrush}" />
    </Style>
    <ControlTemplate x:Key="ThemedMenuItemControlTemplate" TargetType="{x:Type MenuItem}">
        <Grid>
            <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="1" Padding="{TemplateBinding Padding}">
                <DockPanel>
                    <Path x:Name="SubmenuArrow" Data="M 0 0 L 4 4 L 0 8 Z" Fill="{TemplateBinding Foreground}" Width="5" Height="9" Margin="10,0,0,0" DockPanel.Dock="Right" Visibility="Collapsed" />
                    <ContentPresenter ContentSource="Header" RecognizesAccessKey="True" VerticalAlignment="Center" />
                </DockPanel>
            </Border>
            <Popup x:Name="PART_Popup" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" Placement="Bottom" AllowsTransparency="True" PopupAnimation="Fade">
                <Border Background="{DynamicResource MenuItemBackgroundBrush}" BorderBrush="{DynamicResource GridBorderBrush}" BorderThickness="1" Padding="2">
                    <StackPanel IsItemsHost="True" />
                </Border>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="HasItems" Value="True"><Setter TargetName="SubmenuArrow" Property="Visibility" Value="Visible" /></Trigger>
            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource MenuItemHoverBrush}" /><Setter Property="Foreground" Value="{DynamicResource MenuItemHoverForegroundBrush}" /></Trigger>
            <Trigger Property="IsHighlighted" Value="True"><Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource MenuItemSelectedBrush}" /><Setter Property="Foreground" Value="{DynamicResource MenuItemSelectedForegroundBrush}" /></Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True"><Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource MenuItemSelectedBrush}" /><Setter Property="Foreground" Value="{DynamicResource MenuItemSelectedForegroundBrush}" /></Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="{DynamicResource MenuItemBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource MenuItemForegroundBrush}" />
        <Setter Property="Padding" Value="10,5" />
        <Setter Property="Template" Value="{StaticResource ThemedMenuItemControlTemplate}" />
    </Style>
</ResourceDictionary>