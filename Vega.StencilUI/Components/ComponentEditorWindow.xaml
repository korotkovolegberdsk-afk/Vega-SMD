<Window x:Class="Vega.StencilUI.Components.ComponentEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактор компонента (MPN)" Width="560" Height="580" MinWidth="480" MinHeight="480"
        WindowStartupLocation="CenterOwner">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Сохранить" Width="110" Click="Save_Click" />
            <Button Content="Отмена" Width="110" Margin="8,0,0,0" IsCancel="True" />
        </StackPanel>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="180" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Text="Производитель" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,10" />
                <TextBox x:Name="ManufacturerTextBox" Grid.Column="1" Margin="0,0,0,10" />
                <TextBlock Grid.Row="1" Text="MPN" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,10" />
                <TextBox x:Name="MpnTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" />
                <TextBlock Grid.Row="2" Text="Описание" FontWeight="SemiBold" VerticalAlignment="Top" Margin="0,0,12,10" />
                <TextBox x:Name="DescriptionTextBox" Grid.Row="2" Grid.Column="1" Height="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10" />
                <TextBlock Grid.Row="3" Text="Связанный корпус (Package)" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,10" />
                <ComboBox x:Name="PackageComboBox" Grid.Row="3" Grid.Column="1" DisplayMemberPath="PackageName" SelectedValuePath="Id" Margin="0,0,0,10" />
                <TextBlock Grid.Row="4" Text="Путь к preview-изображению" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,0" />
                <TextBox x:Name="PreviewImagePathTextBox" Grid.Row="4" Grid.Column="1" ToolTip="Путь к изображению компонента для карточки MPN" />
            </Grid>
        </ScrollViewer>
    </DockPanel>
</Window>