<Window x:Class="Vega.StencilUI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Vega.StencilUI.ViewModels"
        Title="{DynamicResource SettingsTitle}"
        Height="250"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.DataContext>
        <vm:SettingsViewModel />
    </Window.DataContext>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="{DynamicResource SettingsTitle}" FontSize="18" FontWeight="SemiBold" />
        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{DynamicResource LanguageLabel}" VerticalAlignment="Center" />
            <ComboBox Grid.Column="1" ItemsSource="{Binding Languages}" DisplayMemberPath="DisplayName" SelectedItem="{Binding SelectedLanguage}" />
            <TextBlock Grid.Row="1" Text="{DynamicResource ThemeLabel}" VerticalAlignment="Center" Margin="0,12,0,0" />
            <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Themes}" SelectedItem="{Binding SelectedTheme}" Margin="0,12,0,0" />
        </Grid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource Save}" Width="95" Margin="0,0,8,0" Click="Save_Click" />
            <Button Content="{DynamicResource Cancel}" Width="95" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>